# Exported Patches

These patches were generated from local commits and can be applied on your local repos.

## Files

- `xyzw-analyzer-passive-capture.patch`
- `game-mitm-linux-gosysproxy.patch`

## Apply

```bash
cd /path/to/xyzw_analyzer
git am /path/to/xyzw-analyzer-passive-capture.patch

cd /path/to/game-mitm
git am /path/to/game-mitm-linux-gosysproxy.patch
```

## Notes

- `xyzw_analyzer` commit: `733d2c1`
- `game-mitm` commit: `bdfcd72`
