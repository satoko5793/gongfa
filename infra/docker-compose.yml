version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: gongfa
      POSTGRES_USER: gongfa
      POSTGRES_PASSWORD: gongfa
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
